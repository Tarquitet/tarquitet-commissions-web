---
---
<nav class="hidden md:flex gap-8 items-center">
  <a href="#about" class="text-xs font-bold text-brand-light/60 hover:text-brand-red transition-all tracking-widest">INICIO</a>
  <a href="#content-section" class="text-xs font-bold text-brand-light/60 hover:text-brand-red transition-all tracking-widest tab-trigger" data-tab="gallery">COMISIONES</a>
  <a href="#content-section" class="text-xs font-bold text-brand-light/60 hover:text-brand-red transition-all tracking-widest tab-trigger" data-tab="pricing">PRECIOS</a>
  <a href="#content-section" class="text-xs font-bold text-brand-light/60 hover:text-brand-red transition-all tracking-widest tab-trigger" data-tab="tos">TOS</a>
  <a href="#contact" class="text-xs font-bold text-brand-light/60 hover:text-brand-red transition-all tracking-widest">CONTACTO</a>
</nav>

<script>
  // Script para que el menú pueda avisar al Hero qué pestaña abrir
  const triggers = document.querySelectorAll('.tab-trigger');
  triggers.forEach(t => {
    t.addEventListener('click', () => {
      const tab = t.getAttribute('data-tab');
      // Despachamos un evento personalizado global
      window.dispatchEvent(new CustomEvent('change-tab', { detail: { tab } }));
    });
  });
</script>